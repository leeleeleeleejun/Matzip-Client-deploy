pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm lint
      stage_fixed: true

pre-push:
  parallel: true
  commands:
    audit-check:
      run: pnpm audit
    # 테스트 도입 전까지 비활성화
    # type-check:
    #   run: pnpm run type-check
    # unit-test:
    #   run: pnpm run test -- --passWithNoTests

commit-msg:
  commands:
    lint-commit-msg:
      run: pnpm exec commitlint --edit {1}
